{
    "name": "qstate",
    "private": true,
    "scripts": {
        "publish-packages": "turbo run --filter=@qstate/* build lint && changeset version && changeset publish",
        "build": "turbo build",
        "dev": "turbo dev",
        "lint": "turbo lint",
        "format": "prettier --write \"**/*.{ts,tsx,md}\""
    },
    "devDependencies": {
        "prettier": "latest",
        "turbo": "^2.3.3",
        "@changesets/cli": "^2.27.11",
        "typescript": "latest"
    },
    "packageManager": "pnpm@9.0.0",
    "engines": {
        "node": ">=18"
    }
}
